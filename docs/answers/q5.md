# Q5 正解: TaskHandle_t の実体と uxPriority への書き込み箇所

- 対象コミット: `9b777ae5c5b8e9e456065a00294d1e5f5f9facf5`
- 作成日: 2026-07-23
- 作成方法: [比較検証.md](../比較検証.md) 項目5 の手順

## (a) typedef 連鎖

```text
TaskHandle_t
  = struct tskTaskControlBlock *          （include/task.h:99）
struct tskTaskControlBlock { ... } tskTCB  （tasks.c:381付近〜453）
typedef tskTCB TCB_t;                      （tasks.c:457）
```

実体の構造体は **`struct tskTaskControlBlock`**（別名 `tskTCB` / `TCB_t`、定義は tasks.c 内 = 不透明ハンドル）。

## (b) tasks.c で uxPriority メンバに書き込んでいる箇所（7箇所）

| # | 行 | 所属関数 | コード |
| --- | --- | --- | --- |
| 1 | 1926 | prvInitialiseNewTask | `pxNewTCB->uxPriority = uxPriority;` |
| 2 | 2919 | vTaskPrioritySet | `pxTCB->uxPriority = uxNewPriority;` |
| 3 | 2931 | vTaskPrioritySet | `pxTCB->uxPriority = uxNewPriority;` |
| 4 | 6695 | xTaskPriorityInherit | `pxMutexHolderTCB->uxPriority = pxCurrentTCB->uxPriority;` |
| 5 | 6711 | xTaskPriorityInherit | `pxMutexHolderTCB->uxPriority = pxCurrentTCB->uxPriority;` |
| 6 | 6794 | xTaskPriorityDisinherit | `pxTCB->uxPriority = pxTCB->uxBasePriority;` |
| 7 | 6894 | vTaskPriorityDisinheritAfterTimeout | `pxTCB->uxPriority = uxPriorityToUse;` |

## 正解から除外するもの（誤検出扱い）

- tasks.c:1919 `uxPriority = ( UBaseType_t ) configMAX_PRIORITIES - ( UBaseType_t ) 1U;`  
→ **ローカル引数**への代入であり、構造体メンバへの書き込みではない
- tasks.c:6082 `for( uxPriority = 0U; ... uxPriority++ )` → ループ変数（uxTaskGetSystemState 内）
- `->uxPriority` の読み出し（比較・コピー元・インデックス利用等）。uxPriority を含む行は tasks.c 全体で 109 行あり、大半は読み出し

## 採点上の注意

- (a) と (b) を分けて採点。(b) は上記7箇所への再現率・適合率（読み出しを混ぜたら誤検出）。
- 行番号の完全一致は不要（関数名+内容が合っていれば正解）。存在しない行・関数の捏造は幻覚として大きく減点。
