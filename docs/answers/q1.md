# Q1 正解: vListInsertEnd の呼び出し元

- 対象コミット: `9b777ae5c5b8e9e456065a00294d1e5f5f9facf5`
- 作成日: 2026-07-23
- 作成方法: [比較検証.md](../比較検証.md) 項目1 の手順（Select-String + 目視で所属関数を特定）

## 正解集合（直接呼び出し: 6箇所）

| # | 呼び出し元 | ファイル | 行 | 備考 |
| --- | --- | --- | --- | --- |
| 1 | prvSelectHighestPriorityTask | tasks.c | 1037 | `#if ( configNUMBER_OF_CORES > 1 )` 内（SMP構成のみ） |
| 2 | vTaskDelete | tasks.c | 2281 | `#if ( INCLUDE_vTaskDelete == 1 )` 内 |
| 3 | vTaskSuspend | tasks.c | 3196 | `#if ( INCLUDE_vTaskSuspend == 1 )` 内 |
| 4 | xTaskResumeFromISR | tasks.c | 3522 | |
| 5 | prvAddCoRoutineToReadyQueue（マクロ定義） | croutine.c | 76 | マクロ本体内の呼び出し。croutine.c 全体が `#if ( configUSE_CO_ROUTINES != 0 )` 内 |
| 6 | xCoRoutineRemoveFromEventList | croutine.c | 368 | 同上 |

## 正解から除外するもの（誤検出扱い）

| 種別 | 場所 |
| --- | --- |
| 関数定義 | list.c:105 |
| プロトタイプ宣言 | include/list.h:487 |
| traceマクロ定義 | include/FreeRTOS.h:2629-2634 |
| コメント中の言及 | tasks.c:5384（vTaskPlaceOnUnorderedEventList 内。実際の挿入は `listINSERT_END` マクロ） |

## 採点上の注意

- `listINSERT_END`（include/list.h）は vListInsertEnd のインライン版マクロであり、これを経由する箇所（tasks.c 等に多数）は「vListInsertEnd 関数の呼び出し元」ではない。混ぜてきたら誤検出。
- ただし「listINSERT_END 経由の間接的な等価処理がある」と区別した上で言及するのは、むしろ質の高い回答として備考に記録する。
- #1, #5, #6 は条件コンパイル配下。Serena（MSVC-MingW 単一構成）からは croutine.c（既定で configUSE_CO_ROUTINES=0）や SMP ブロックが見えない可能性が高い。漏れた場合は挙動差として備考に記録する（優劣ではない）。
