# Q2 正解: xSemaphoreGive の実体と内部呼び出し

- 対象コミット: `9b777ae5c5b8e9e456065a00294d1e5f5f9facf5`
- 作成日: 2026-07-23
- 作成方法: [比較検証.md](../比較検証.md) 項目2 の手順

## (a) 実体関数

`include/semphr.h:460`

```c
#define xSemaphoreGive( xSemaphore )    xQueueGenericSend( ( QueueHandle_t ) ( xSemaphore ), NULL, semGIVE_BLOCK_TIME, queueSEND_TO_BACK )
```

実体は **`xQueueGenericSend`**（queue.c:949 で定義、本体は 949〜1164 行）。  
マクロ展開は1段のみ（`semGIVE_BLOCK_TIME` は `( TickType_t ) 0U`、`queueSEND_TO_BACK` は挿入位置指定の定数）。

## (b) xQueueGenericSend の内部から呼ばれる関数（3つ挙げられれば正解）

関数（この中から3つ挙げていれば正解）:

| 関数 | 定義場所 |
| --- | --- |
| prvCopyDataToQueue | queue.c（static） |
| prvIsQueueFull | queue.c（static） |
| prvUnlockQueue | queue.c（static） |
| prvNotifyQueueSetContainer | queue.c（static、configUSE_QUEUE_SETS 有効時のパス） |
| vTaskSuspendAll | tasks.c |
| xTaskResumeAll | tasks.c |
| vTaskInternalSetTimeOutState | tasks.c |
| xTaskCheckForTimeOut | tasks.c |
| vTaskPlaceOnEventList | tasks.c |
| xTaskRemoveFromEventList | tasks.c |
| xTaskGetSchedulerState | tasks.c |

マクロ（関数としてカウントしないが、挙げてきても誤りとまでは扱わない。備考に記録）:
`prvLockQueue` / `taskENTER_CRITICAL` / `taskEXIT_CRITICAL` / `queueYIELD_IF_USING_PREEMPTION` / `taskYIELD_WITHIN_API` / `listLIST_IS_EMPTY` / `configASSERT` / trace系

## 採点上の注意

- (a) を外した時点で 0 点（例: `xQueueGive` など存在しない関数名、`xQueueGiveMutexRecursive`（これは xSemaphoreGiveRecursive の展開先）との混同）。
- `prvLockQueue` は queue.c では**マクロ**である点に注意（関数と答えたら軽微な誤り）。
