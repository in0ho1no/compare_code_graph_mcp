
# 第2部 機能比較（検証項目1〜6）

## 検証用の共通準備

Windows + Claude Code を前提とします（WSL2 でも可。以下は PowerShell 表記）。

```powershell
# 検証用ディレクトリにリポジトリを取得（タグ固定で再現性を確保）
git clone --branch V11.3.0 --depth 1 https://github.com/FreeRTOS/FreeRTOS-Kernel.git
cd FreeRTOS-Kernel
```

> タグは検証時点の最新安定版を `git ls-remote --tags` で確認して固定してください。**両ツールとも同一コミットをインデックスする**ことが比較の大前提です。

## 5. 比較の実施ルール（先に決めておく）

結果の再現性のため、以下を固定します。

1. **同一コミット・同一エージェント・同一モデル**（Claude Code のモデルとバージョンを記録）
2. **クエリごとに新規セッション**で実施（前の回答がコンテキストに残ると後続クエリが有利になるため）
3. **比較対象の MCP は一方だけ有効化**（`.mcp.json` を切り替える。両方有効だとエージェントが混ぜて使う）
4. **内蔵ツールへのフォールバックを禁止する**。素の Grep/Read で答えられてしまうと MCP の比較にならないため、全プロンプトに以下の前置きを付けます。

   > このリポジトリに関する質問です。回答には MCP ツール（serena / codebase-memory-mcp）のみを使用し、内蔵の Grep・Glob・Read・Bash は使用しないでください。ツールで確認できなかった内容は「不明」と答えてください。推測で補完しないでください。

   ※ この禁止は **MCP ツール単体の性能を測るための実験統制**であり、実運用の推奨設定ではありません。実運用での使い分けは付録Aを参照。

5. **各クエリ3回実施**し、中央値を採用（LLM 側の揺らぎを均すため）
6. 記録項目: 正答性 / ツール呼び出し回数 / トークン消費（`/cost` または `/context` の差分）/ 所要時間 / フォールバック発生の有無

## 6. 検証項目の詳細

各項目は「目的 → 投入プロンプト → 正解データの作成手順 → 採点基準」の順です。正解データは**検証前に**作成し、`answers/` ディレクトリに保存しておきます。

### 項目1: クロスファイル参照（ベースライン）

**目的**: 最も基本的な「この関数の呼び出し元は誰か」。grep でも解ける難度で、両者が満点を取れることを確認する足切り問題。

**プロンプト**:
> `vListInsertEnd` 関数を呼び出している関数をすべて列挙してください。呼び出し元の関数名とファイル名をセットで挙げてください。

**正解の作成**:

```bash
grep -rn "vListInsertEnd" --include="*.c" --include="*.h" .
```

出力から「定義（list.c）」「プロトタイプ宣言（list.h）」を除外し、呼び出し行だけを残す。各呼び出し行がどの関数の中にあるかをエディタで目視特定し、`answers/q1.md` に「呼び出し元関数名 / ファイル / 行番号」の表として保存。

**採点**: 正解集合に対する再現率（漏れ）と適合率（誤検出）を記録。定義や宣言を「呼び出し元」として混ぜたら誤検出扱い。

### 項目2: マクロ・エイリアスの解決

**目的**: FreeRTOS の公開 API はマクロ包装が多い（`xSemaphoreGive` は実体が `xQueueGenericSend` の呼び出しマクロ）。プリプロセス前を見る tree-sitter とプリプロセス後を見る clangd の差が最も出やすい項目。CC-RX 実案件コードへの外挿性が高い。

**プロンプト**:
> `xSemaphoreGive` の実体は最終的にどの関数ですか。マクロ展開を追って実体の関数名を特定し、さらにその実体関数の内部から呼び出される関数を3つ挙げてください。

**正解の作成**:

```bash
grep -n "define xSemaphoreGive" include/semphr.h
# → 展開先の関数名を確認（queue.c 内の実装関数）
grep -n "xQueueGenericSend" queue.c | head
```

`include/semphr.h` の `#define` を目視で辿り、実体関数と、その関数本体から呼ばれる関数（`queue.c` を目視）を `answers/q2.md` に記録。

**採点**: (a) 実体関数名の特定 (b) 内部呼び出し関数の列挙、を分けて採点。(a) を外した時点で 0 点。

### 項目3: 条件コンパイルの理解

**目的**: config マクロによる可視性の判定。`--preprocess` を検討している `mtpj_deps.py` と同じ問題意識で、実務価値が高い。

**プロンプト A**:
> `vTaskDelete` 関数はどの設定マクロが定義されているときに存在しますか。該当する `#if` 条件を答えてください。

**プロンプト B**:
> `configUSE_TIMERS` を 0 にした場合、`timers.c` で定義される公開関数のうちコンパイル対象から外れるものを列挙してください。

**正解の作成**:

```bash
grep -n "INCLUDE_vTaskDelete" tasks.c include/task.h
grep -n "configUSE_TIMERS" timers.c | head -5   # ファイル全体を囲う #if を目視確認
grep -n "^[A-Za-z].*(" timers.c   # 関数定義の一覧づくりの補助
```

timers.c 冒頭の `#if ( configUSE_TIMERS == 1 )` の範囲に含まれる関数定義を目視列挙し `answers/q3.md` へ。

**採点**: A は条件式の正誤（完全一致でなくても意味が合っていれば正解）。B は列挙の再現率・適合率。「Serena は compile_commands.json の構成でプリプロセス済みのため、無効側のコードが見えない/逆に確定的に答えられる」等、**両者の答え方の質の違いを備考欄に必ず記録**する。

### 項目4: 同名シンボルの多重定義（portable/）

**目的**: `pxPortInitialiseStack` は portable/ 以下の各ポートに多数定義がある。全候補を列挙して曖昧さを提示できるか、1つを勝手に選ぶかを見る。ここは正解が1つではなく、**挙動の違いを記録する項目**。

**プロンプト**:
> `pxPortInitialiseStack` 関数の定義はこのリポジトリのどこにありますか。複数ある場合はすべて挙げてください。

**正解の作成**:

```bash
grep -rln "pxPortInitialiseStack" portable/ --include="*.c" | sort > answers/q4_files.txt
wc -l answers/q4_files.txt   # 定義ファイル数を記録
```

**採点**: 列挙数 / 全定義数を記録。予想される挙動: codebase-memory-mcp は全ポートを列挙、Serena は compile_commands.json に含まれるポート（POSIX）1件のみ。**どちらも「仕様通り」であり優劣ではない**ので、採点表では点数ではなく挙動として記録し、「単一構成の正確なビューと全網羅ビューのどちらが自分のユースケースに合うか」の判断材料にする。

### 項目5: 不透明ハンドル → 構造体の解決

**目的**: `TaskHandle_t` → `struct tskTaskControlBlock *` → `TCB_t` の typedef 連鎖を辿り、さらにメンバアクセスの read/write を区別できるか。型解決の質が最もはっきり出る。

**プロンプト**:
> `TaskHandle_t` の実体の構造体は何ですか。typedef の連鎖を示した上で、その構造体の `uxPriority` メンバに**代入（書き込み）**している箇所を tasks.c からすべて挙げてください。

**正解の作成**:

```bash
grep -n "tskTaskControlBlock" include/task.h tasks.c | head
grep -n "uxPriority" tasks.c > /tmp/q5_all.txt
```

`/tmp/q5_all.txt` の各行を目視し「書き込み（`=` の左辺、`++` 等）」と「読み出し」に分類。書き込み行のみ `answers/q5.md` へ。

**採点**: (a) typedef 連鎖の正しさ (b) 書き込み箇所の再現率・適合率（読み出しを混ぜたら誤検出）。

### 項目6: 関数ポインタ経由の呼び出し（限界確認）

**目的**: タイマコールバックのような間接呼び出しは静的解析の限界域。両者とも完答できない可能性が高く、**「できないことをどう答えるか」の質**（不明と言えるか、幻覚を出すか）を比較する。

**プロンプト**:
> ソフトウェアタイマのコールバック関数はどこで登録され、どこで実行（呼び出し）されますか。登録箇所と実行箇所をファイル名・関数名で答えてください。

**正解の作成**:

```bash
grep -n "pxCallbackFunction" timers.c include/timers.h
```

登録箇所（`xTimerCreate` 系での格納）と実行箇所（`prvProcessExpiredTimer` 等での間接呼び出し）を目視特定し `answers/q6.md` へ。

**採点**: 登録・実行それぞれの特定可否に加え、**幻覚（存在しない関数名・行の捏造）が出たら大きく減点**。「グラフ/LSP では追えない」と正直に答えた場合は部分点。

## 7. 採点表テンプレート

`results/` に以下の Markdown（または Excel）を用意し、1クエリ×1ツール×1試行を1行として記録します。

### 7.1 試行記録シート

| # | クエリ | ツール | 試行 | 正答性 | ツール呼出回数 | トークン | 時間 | フォールバック | 備考 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| 1 | Q1 参照検索 | Serena | 1/3 | 1.0 | 3 | 4.2k | 40s | なし | |
| 2 | Q1 参照検索 | CBM | 1/3 | 1.0 | 1 | 0.8k | 12s | なし | |
| … | | | | | | | | | |

記入ルール:

- **正答性**: 1.0（完全一致）/ 0.5（漏れ・誤検出が軽微、または部分回答）/ 0（実体を外した・幻覚あり）。項目4は点数の代わりに「列挙数/全数」を記載。
- **トークン**: セッション開始直後と回答完了後の `/cost`（または `/context`）の差分。
- **フォールバック**: 指示に反して内蔵 Grep/Read を使ったら「あり」とし、その試行は参考値扱い（やり直し推奨）。

### 7.2 集計サマリ（クエリ別中央値）

| クエリ | 観点 | Serena | codebase-memory-mcp | 所見 |
| --- | --- | --- | --- | --- |
| Q1 参照検索 | 正答性 / 呼出 / トークン | | | |
| Q2 マクロ解決 | 〃 | | | |
| Q3 条件コンパイル | 〃 | | | |
| Q4 多重定義 | 列挙数 / 挙動 | | | 優劣でなく挙動差を記録 |
| Q5 型解決+R/W | 正答性 / 呼出 / トークン | | | |
| Q6 間接呼出 | 正答性 / 幻覚有無 | | | 限界の質の比較 |
| 共通 | セットアップ工数（実測時間） | | | 4.1 / 4.2 の実測 |
| 共通 | インデックス時間・DB/キャッシュサイズ | | | |

### 7.3 判定の目安

- Q1 で満点を取れないツールはその時点で採用外。
- 実務（CC-RX プロジェクト）への外挿は **Q2・Q3 の結果を最重視**する（マクロ包装・条件コンパイルだらけのため）。
- ただし Serena の Q2・Q3 の好成績は「compile_commands.json がある前提」の成績である点に注意。**CS+/CC-RX 案件では compile_commands.json の生成自体が課題**なので、最終判断は「Q2・Q3 の性能差」×「自案件で clangd を成立させるコスト」の掛け算で行う。

## 8. 実施の流れ（チェックリスト）

- [ ] FreeRTOS-Kernel をタグ固定で clone、コミット SHA を記録
- [ ] `answers/` に Q1〜Q6 の正解データを作成（grep + 目視、実施前に完了させる）
- [ ] codebase-memory-mcp をセットアップ（4.1）、インデックス時間と DB サイズを記録
- [ ] Serena をセットアップ（4.2）、compile_commands.json の生成方法・セットアップ所要時間を記録
- [ ] `.mcp.json` を CBM 側に切替、`check_index_freshness.ps1` で鮮度確認 → Q1〜Q6 を各3回（毎回新規セッション）
- [ ] `.mcp.json` を Serena 側に切替 → 同様に実施
- [ ] 7.1 に全試行を記録、7.2 に中央値を集計
- [ ] 所見をまとめ、CS+ 実案件への適用方針（どちらを常用するか / 併用するか / clangd 整備に投資するか）を結論として記載

## 9. 参考リンク

- FreeRTOS-Kernel: <https://github.com/FreeRTOS/FreeRTOS-Kernel>
- Serena: <https://github.com/oraios/serena> （C/C++ サポート: <https://oraios.github.io/serena/01-about/020_programming-languages.html> ）
- codebase-memory-mcp: <https://github.com/DeusData/codebase-memory-mcp> （論文: arXiv:2603.27277）
- MCP 仕様: <https://modelcontextprotocol.io/>

---
